# MIGRATION NOTE v1.1

日期：2026-02-11  
適用來源：`語魂系統GPTs/`、`語魂系統GPTs_Codex版/`  
目標：收斂為單一可追溯版本 `語魂系統GPTs_v1.1/`

## 1. 遷移原則

1. 單一真相來源：術語與門檻只認 `01_術語與門檻單一規格.md`。  
2. 可追溯優先：任何宣稱都要能映射到實際 repo 路徑。  
3. 先收斂再擴充：先消除歧義，再新增功能性內容。

## 2. 來源整併策略

- 哲學敘事主體：主要承接 `語魂系統GPTs/`。
- 工程規格骨架：主要承接 `語魂系統GPTs_Codex版/`。
- 對外簡報與講稿：承接 Codex 版 `07/08/09`，改為 v1.1 術語。

## 3. 術語遷移對照

- `ΔS(舊：漂移)` -> `ΔΣ`（Semantic Divergence）
- `ΔS(新：方向)` -> 保留為 Direction（`[-1,1]`）
- `POAV` 雙詞義 -> 單詞義（Parsimony/Orthogonality/Audibility/Verifiability）

## 4. Gate 與門檻遷移

- 統一輸出：`PASS / REWRITE / BLOCK`
- 統一門檻：
  - `POAV >= 0.70` -> `PASS`
  - `0.30~0.69` -> `REWRITE`
  - `< 0.30` -> `BLOCK`
  - 高風險任務需 `>= 0.90`

## 5. 路徑遷移規則

1. 舊文件引用 `body/*` 視為失效路徑，需改寫。  
2. 所有檔案路徑在合併後必須通過 `Test-Path`。  
3. 無對應實作的敘述需標記為概念性，不可偽裝成已落地。

## 6. 人工審計最小流程

1. 先讀 `00_差異矩陣.md` 了解衝突與處理策略。  
2. 再讀 `01_術語與門檻單一規格.md` 鎖定唯一定義。  
3. 抽查 `10_哲學到程式_可驗證對照表.md` 的測試入口與證據欄。  
4. 用 `11_一頁式審計清單_給人類看的.md` 進行人工打分。  
5. 需要對外說明時使用 `14~16`。

## 7. 破壞性變更提醒（Breaking Changes）

- 任何沿用舊 `ΔS=漂移` 的流程都會在 v1.1 判定為術語不合規。
- 任何同時保留雙 POAV 定義的文件都不再被視為 authoritative。
- 依賴失效路徑的流程描述需重寫後才能納入審計。

## 8. 完成定義（Done Criteria）

- [x] `v1.1` 文件補齊（`00~16` + 2 份收尾文件）
- [x] 術語與門檻無多版本衝突
- [x] 文件路徑引用可被掃描驗證
- [x] 人類可讀審計入口可直接使用
