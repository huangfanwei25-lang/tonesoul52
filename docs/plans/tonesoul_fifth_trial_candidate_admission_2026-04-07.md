# ToneSoul Fifth Trial Candidate Admission (2026-04-07)

> Purpose: admit one fifth bounded self-improvement candidate after the fourth trial result is registered.
> Status: admitted candidate note for `Phase 808`.
> Authority: planning aid. Runtime truth remains in code, tests, accepted contracts, and the current bounded trial/result surfaces.

---

## Why This Exists

ToneSoul now has:

- one promoted consumer-parity packaging result
- one parked operator-retrieval cueing result
- one promoted deliberation-hint packaging result
- one promoted task-board parking clarity result
- one promoted shared-edit overlap clarity result

That means the next bottleneck is no longer:

- whether path-overlap packaging can be made clearer
- whether the self-improvement loop can promote multiple bounded packaging wins honestly

It is:

`which next bounded candidate most directly reduces outward-action ambiguity without inflating launch maturity or creating a new release-permission myth?`

---

## Admission Rule

The fifth candidate must satisfy all of these:

- stay inside `allowed_now` mutation classes
- reduce a live outward-action friction already visible in this repo
- preserve consumer parity across Codex-style, Claude-style, dashboard, and observer-facing shells
- keep rollback posture explicit
- avoid inventing automatic publish authority, release automation, or launch-tier inflation

If it cannot satisfy those, it does not enter the fifth wave.

---

## Chosen Candidate

### `candidate_id`

`publish_push_posture_clarity_v1`

### `target_surface`

`publish_push_preflight.result_shape`

### `target_consumer`

`codex_cli / claude_adapter / dashboard_operator_shell`

---

## Why This Candidate

This is the best fifth candidate because it sits at the exact friction point where later agents still risk:

- reading any publish/push caution as a hard stop
- ignoring launch-honesty cues because they are mixed into generic review reasons
- over-reading a packaging improvement as if it authorized stronger release actions

It is also firmly inside an already-allowed mutation class:

- packaging and bounded preflight readouts

And it avoids the parked temptations:

- no retrieval-runtime optimism
- no deployment automation
- no launch-tier or governance rewrite

---

## Baseline Story

ToneSoul already has:

- `publish_push_preflight`
- `run_publish_push_preflight.py`
- bounded `clear / review_before_push / blocked` outcomes

That is already useful.

But one remaining friction is still visible:

`later agents can still see one blended review list and miss the difference between hard review cues and launch-honesty posture cues.`

That ambiguity does not break the system, but it taxes bounded collaboration and release discipline.

---

## Candidate Story

The candidate is:

- not deployment automation
- not a release-permission system
- not a launch-tier rewrite
- not a public-claim maturity upgrade

It is only a packaging refinement of:

- `publish_push_preflight`

The likely direction is:

- surface `decision_basis`
- separate bounded `review_cues`
- separate bounded `honesty_cues`
- keep the current final classification and recommended command

This keeps the candidate narrow and testable.

---

## Proposed Success Metric

The fifth-wave evaluator should judge this candidate using:

1. regression:
   - `tests/test_publish_push_preflight.py`
   - `tests/test_run_publish_push_preflight.py`

2. parity:
   - the new packaging remains understandable from Codex-style, Claude-style, and dashboard/operator shells
   - the self-improvement drift wave still stays aligned

3. operability:
   - a later agent can distinguish:
     - hard blocked reasons
     - review posture cues
     - launch-honesty cues
   - without treating any of those as new authority tiers

4. honesty:
   - the result must not imply stronger push or publish permission

---

## Failure Watch

The main failure watches are:

- packaging that sounds clearer but still hides whether the real friction is release risk or wording honesty
- new fields that quietly look like publish permission
- shell drift where one consumer reads the new shape as stronger authority than another
- adding extra structure without reducing actual ambiguity

If these dominate, the fifth wave should end in:

- `park`
- or `retire`

not `promote`.

---

## Rollback Posture

`bounded_restore`

If the trial fails:

- restore the prior `publish_push_preflight` packaging
- keep the result surface as history only
- do not reinterpret the failure as evidence that publish/push preflight itself was a bad idea

---

## Overclaim To Avoid

`better publish/push posture clarity is not better launch maturity`

This candidate, even if promoted, does **not** prove:

- safer deployments
- stronger release authority
- better launch readiness
- improved human approval

It would only prove:

- clearer bounded packaging for publish/push posture

---

## No-Go List For Wave 5

These should **not** enter the fifth wave:

1. any deployment automation or auto-push feature
   - outside the current mutation boundary

2. any public-launch optimism based on packaging alone
   - still forbidden

3. governance, identity, vow, or transport changes
   - outside allowed mutation classes

4. shell redesign framed as "publish clarity"
   - too broad for the current phase

5. retrieval-runtime or compiled-knowledge optimism
   - still parked

---

## Recommended Next Phase

Open:

- `Phase 809: Fifth Bounded Trial Wave - Publish/Push Posture Clarity`

Do **not** open:

- deployment automation
- public-launch claim escalation
- release-permission mythology
