# ToneSoul Eleventh Trial Candidate Admission (2026-04-07)

> Purpose: admit one eleventh bounded self-improvement candidate after the tenth trial result is classified and registered.
> Scope: self-improvement v0 only. This note keeps the candidate inside consumer-shell packaging and away from planner, permission, transport, or governance mythology.

---

## Current Registry Snapshot

The bounded registry now shows:

- `promote=10`
- `park=1`

Promoted families now cover:

- consumer first-hop packaging
- deliberation latency cueing
- task-board parking clarity
- shared-edit overlap clarity
- publish/push posture clarity
- mutation follow-up routing
- surface-versioning lineage clarity
- launch-health trend clarity
- internal-state action clarity
- hook-chain trigger clarity

Parked family:

- `operator_retrieval_cueing_v1`

So the next candidate should:

- stay inside an allowed-now class
- improve cross-shell interpretation parity
- reduce one remaining successor-facing guess that still exists even after first-hop order is aligned

---

## Admitted Candidate

`consumer_misread_guard_clarity_v1`

Target surface:

- `consumer_contract.priority_misread_guard`

Why this candidate:

- the consumer contract already stabilized read order, but misread guards were still mostly `name + rule`
- Codex-style shells, Claude-style shells, and dashboard shells still had to infer:
  - where the misread would first appear
  - what corrective action to apply
  - which correction mattered most now
- the candidate stays inside consumer packaging only and directly targets the Codex/Claude interpretation gap

---

## Baseline Story

`consumer_contract` already exposed:

- required first-hop order
- compatible consumers
- source precedence summary
- guard names and rules

But it still left one operator burden:

- infer which surface a guard referred to
- reconstruct the corrective action from general rule text
- guess which guard to foreground when closeout or readiness pressure changed

That meant parity existed, but the correction grammar was still thinner than the read-order grammar.

---

## Success Metric

Promote only if:

- every guard exposes `trigger_surface`
- every guard exposes `operator_action`
- one bounded `priority_misread_guard` is visible
- the Claude-style adapter preserves the same first correction
- the change adds no new planner, no new permission, and no transport-story widening

---

## Rollback Posture

If the trial fails:

- remove `priority_misread_guard`
- remove the extra trigger/action packaging
- restore the prior `name + rule` guard list
- keep the lesson only in the dedicated self-improvement status surface

---

## Overclaim To Avoid

Do **not** say this means:

- stronger cross-vendor transport
- shared cognition
- safer mutation by itself
- a new planner or policy engine

This is only clearer consumer-shell correction packaging.

---

## No-Go List

This admission does **not** reopen:

- transport semantics
- readiness truth or claim truth
- new hook families
- governance or identity layers
- retrieval/runtime truth mixing
