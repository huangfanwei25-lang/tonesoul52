# ToneSoul Tenth Trial Candidate Admission (2026-04-07)

> Purpose: admit one tenth bounded self-improvement candidate after the ninth trial result is classified and registered.
> Scope: self-improvement v0 only. This note keeps the candidate inside hook-shell packaging and away from planner, permission, or authority mythology.

---

## Current Registry Snapshot

The bounded registry now shows:

- `promote=9`
- `park=1`

Promoted families now cover:

- consumer first-hop packaging
- deliberation latency cueing
- task-board parking clarity
- shared-edit overlap clarity
- publish/push posture clarity
- mutation follow-up routing
- surface-versioning lineage clarity
- launch-health trend clarity
- internal-state action clarity

Parked family:

- `operator_retrieval_cueing_v1`

So the next candidate should:

- stay inside an allowed-now class
- improve operator leverage without widening authority
- reduce one remaining successor-facing packaging guess

---

## Admitted Candidate

`hook_chain_trigger_clarity_v1`

Target surface:

- `hook_chain.current_recommendation`

Why this candidate:

- `mutation_preflight.next_followup` already became dynamic, but `hook_chain` still read like a static list
- dashboard/operator shells benefit more if the hook shell itself says which bounded preflight is currently relevant
- the candidate stays inside packaging and consumer-readout space only

---

## Baseline Story

`hook_chain` already exposed:

- three bounded preflight stages
- stage order
- commands
- outcome sets

But it still left one operator burden:

- infer which hook matters now from `mutation_preflight.next_followup`
- reconstruct why that hook is current instead of reading it from the hook shell
- bridge a gap between `stages` and actual tier-0/dashboard badges

That means the hook shell was safe, but not yet as actionable as it could be.

---

## Success Metric

Promote only if:

- live `hook_chain` exposes `hooks`
- one bounded `current_recommendation` is visible when `mutation_preflight.next_followup` is visible
- the recommendation stays aligned with `mutation_preflight.next_followup.target`
- the change adds no new hook, no new permission, and no planner posture

---

## Rollback Posture

If the trial fails:

- remove `current_recommendation`
- remove new hook badges or activation cues
- keep the prior static hook chain
- retain the lesson only in the dedicated self-improvement status surface

---

## Overclaim To Avoid

Do **not** say this means:

- stronger permissions
- autonomous planning
- new hook authority
- safer code mutation by itself

This is only clearer hook-shell packaging.

---

## No-Go List

This admission does **not** reopen:

- new hook families
- path locks or write permissions
- readiness truth or claim truth
- retrieval/runtime truth mixing
- governance or identity layers
