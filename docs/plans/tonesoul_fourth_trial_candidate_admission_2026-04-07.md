# ToneSoul Fourth Trial Candidate Admission (2026-04-07)

> Purpose: admit one fourth bounded self-improvement candidate after the third trial result is registered.
> Status: admitted candidate note for `Phase 805`.
> Authority: planning aid. Runtime truth remains in code, tests, accepted contracts, and the current bounded trial/result surfaces.

---

## Why This Exists

ToneSoul now has:

- one promoted consumer-parity packaging result
- one parked operator-retrieval cueing result
- one promoted deliberation-hint packaging result
- one promoted task-board parking clarity result
- one status surface that can honestly carry promoted versus parked trial results

That means the next bottleneck is no longer:

- whether bounded self-improvement can produce honest promoted results
- whether task-board routing still needs basic packaging repair

It is:

`which next bounded candidate most directly reduces real multi-agent coordination friction without widening mutation authority or reopening retrieval mythology?`

---

## Admission Rule

The fourth candidate must satisfy all of these:

- stay inside `allowed_now` mutation classes
- reduce a live multi-agent coordination friction already visible in this repo
- preserve consumer parity across Codex-style, Claude-style, dashboard, and observer-facing shells
- keep rollback posture explicit
- avoid inventing a new permission system, lock system, or write-authority myth

If it cannot satisfy those, it does not enter the fourth wave.

---

## Chosen Candidate

### `candidate_id`

`shared_edit_overlap_clarity_v1`

### `target_surface`

`shared_edit_preflight.result_shape`

### `target_consumer`

`codex_cli / claude_adapter / dashboard_operator_shell`

---

## Why This Candidate

This is the best fourth candidate because it sits right at the daily friction point where later agents still risk:

- seeing `coordinate` and not knowing whether the real pressure is another agent's overlap or their own missing claim coverage
- over-reading shared-edit caution as a permission system
- under-reading claim discipline after a coordination conflict clears

It is also firmly inside an already-allowed mutation class:

- packaging and bounded preflight readouts

And it avoids the parked temptations:

- no retrieval-runtime optimism
- no governance semantics rewrite
- no identity or hot-memory transport change

---

## Baseline Story

ToneSoul already has:

- `shared_edit_preflight`
- `run_shared_edit_preflight.py`
- bounded `blocked / coordinate / claim_first / clear` decisions

That is already useful.

But one remaining friction is still visible:

`later agents can see the main decision, yet still miss whether they are dealing with other-agent overlap, self-claim gaps, or both at once.`

That ambiguity does not break the system, but it taxes normal collaboration.

---

## Candidate Story

The candidate is:

- not a new write-permission layer
- not an auto-claim mechanism
- not a path-lock system
- not a mutation-authority expansion

It is only a packaging refinement of:

- `shared_edit_preflight`

The likely direction is:

- surface `decision_basis`
- surface bounded `other_overlap_paths`
- surface bounded `claim_gap_paths`
- keep the current final decision and recommended command

This keeps the candidate narrow and testable.

---

## Proposed Success Metric

The fourth-wave evaluator should judge this candidate using:

1. regression:
   - `tests/test_shared_edit_preflight.py`
   - `tests/test_run_shared_edit_preflight.py`

2. parity:
   - the new packaging remains understandable from Codex-style, Claude-style, and dashboard/operator shells
   - the self-improvement drift wave still stays aligned

3. operability:
   - a later agent can distinguish:
     - other-agent overlap
     - self-claim gap
     - both at once
   - without needing a second prose explanation

4. honesty:
   - the result must not imply new authority to write, claim, or bypass coordination

---

## Failure Watch

The main failure watches are:

- packaging that sounds clearer but still hides whether overlap or claim-gap pressure is primary
- new fields that quietly look like permission grants
- shell drift where one consumer reads the new shape as stronger authority than another
- adding extra structure without reducing actual ambiguity

If these dominate, the fourth wave should end in:

- `park`
- or `retire`

not `promote`.

---

## Rollback Posture

`bounded_restore`

If the trial fails:

- restore the prior `shared_edit_preflight` packaging
- keep the result surface as history only
- do not reinterpret the failure as evidence that shared-edit preflight itself was a bad idea

---

## Overclaim To Avoid

`better shared-edit overlap clarity is not better collaboration quality`

This candidate, even if promoted, does **not** prove:

- better human approval
- better planning quality
- broader safe mutation authority
- stronger cross-agent memory transport

It would only prove:

- clearer bounded packaging for shared-edit friction

---

## No-Go List For Wave 4

These should **not** enter the fourth wave:

1. any path-lock or auto-claim feature
   - outside the current mutation boundary

2. retrieval-runtime or compiled-knowledge optimism
   - still parked

3. governance, identity, vow, or transport changes
   - outside allowed mutation classes

4. shell redesign framed as "shared edit clarity"
   - too broad for the current phase

5. public/demo surface changes justified by self-improvement momentum alone
   - not the shortest board

---

## Recommended Next Phase

Open:

- `Phase 806: Fourth Bounded Trial Wave - Shared-Edit Overlap Clarity`

Do **not** open:

- broader mutation authority
- retrieval-runtime experimentation
- new permission mythology
