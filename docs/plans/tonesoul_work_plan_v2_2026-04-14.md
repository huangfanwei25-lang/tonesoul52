# ToneSoul Work Plan v2 (Repo-Aligned)

> Purpose: translate the external Downloads `v1` work-plan draft into a repo-aligned working plan that follows current authority surfaces instead of freezing an older snapshot.
> Status: planning aid only. `task.md`, latest `docs/status/*`, accepted contracts, code, and tests still outrank this file.
> Last Updated: 2026-04-14

---

## Current Truth Anchors

Use these before treating any phase below as active:

1. [task.md](../../task.md)
2. [docs/status/collaborator_beta_preflight_latest.md](../status/collaborator_beta_preflight_latest.md)
3. [docs/status/tonesoul_convergence_audit_latest.md](../status/tonesoul_convergence_audit_latest.md)
4. latest handoff under `docs/status/codex_handoff_*.md`

---

## Current Read

- Main launch line is still `Phase 722 -> Phase 726`, not a new theory lane.
- Current public claim remains guided collaborator beta only; public launch stays deferred.
- Launch-default coordination remains file-backed.
- Knowledge-layer boundary work is already landed through `Phase 796`; do not reopen it as if it were still an unstarted main phase.
- Operator shell remains a subordinate program and should only take one bounded step at a time.
- Current top technical risk is context bloat; runtime seam reduction is a valid side-lane only when it does not steal the launch short board.

---

## Goals

### Goal A

Add one more honest repeated external / lower-context validation sample to the collaborator-beta evidence base.

### Goal B

Keep launch-readiness current truth easy to continue without repo archaeology.

### Goal C

Let operator shell and runtime-cleanup work reduce friction without becoming a shadow mainline.

---

## Guardrails

1. Do not widen public claims beyond the evidence shown in current status surfaces.
2. Do not reopen ontology, domain-core, or public-launch wording while `Phase 722` is still the active launch gap.
3. Do not treat old `docs/plans/*` files as live authority when `task.md` or latest status artifacts say otherwise.
4. Reuse the existing repeated-validation pack and normal session-end path; do not invent a parallel validation flow.
5. If a side-lane document creates a second "current truth" center, stop and collapse it back into the canonical surfaces.

---

## Working Phases

## Phase A: Next Repeated External Cycle

### Purpose

Run the next bounded `Phase 722` validation cycle under a different lower-context or non-creator task shape.

### Scope

- use the existing dual-surface repeated-validation pack
- stay inside collaborator-beta claim boundaries
- finish through official closeout surfaces

### Expected Outputs

- one new `Phase 722`-class status note under `docs/status/`
- one honest closeout via `end_agent_session.py`
- preflight refresh only if the new evidence actually changes launch posture

### Success Conditions

- first-hop path and claim discipline are preserved
- the cycle is auditable without hidden context
- the result is usable whether it is pass, bounded failure, or mixed

## Phase B: Current-Truth Consolidation

### Purpose

Make the launch short board easier for the next agent to continue without re-deriving what is live.

### Scope

- align `task.md`, preflight, go/no-go, and latest handoff
- distinguish current truth from lineage/background
- remove any newly introduced drift or overclaim

### Expected Outputs

- one concise current-truth alignment update
- one explicit next-move decision:
  - continue repeated validation, or
  - move into the next launch-readiness subtopic

## Phase C: One Bounded Operator Shell Step

### Purpose

Reduce operator friction only after the launch-facing surfaces are stable.

### Allowed Targets

- `Phase 769`: first operator workspace adaptation
- `Phase 774`: status panel realignment
- `Phase 780`: bounded operator walkthrough pack
- `Phase 784`: bounded retrieval-preview strip

### Not This

- do not reopen deferred `command shelf` work by default
- do not let dashboard/UI become a second authority center

## Phase D: Context-Bloat Side Lane

### Purpose

Continue shrinking hot runtime context only when it does not block the launch short board.

### Current Safe Shape

- behavior-preserving seam extraction
- keep public API stable
- prefer pure helper clusters over orchestration rewrites

### Current Likely Next Seam

- `_build_operator_guidance`
- then `r_memory_packet`

---

## Not Active Right Now

- reopening knowledge-layer boundary as a new main phase
- ontology or identity rewrite
- public-launch wording expansion
- broad Redis/live-hardening claims

---

## Priority Order

### P0

`Phase A`: next repeated external cycle

### P1

`Phase B`: current-truth consolidation

### P2

`Phase C`: one bounded operator shell step

### P3

`Phase D`: context-bloat seam reduction

---

## Immediate Next Move

1. Run one new lower-context `Phase 722` cycle.
2. Fold the result back into current truth.
3. Only then decide whether the next smallest move belongs to operator shell or runtime seam reduction.
