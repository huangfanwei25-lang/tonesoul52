# ToneSoul Seventh Trial Candidate Admission (2026-04-07)

> Purpose: admit one seventh bounded self-improvement candidate after the sixth trial result is classified and registered.
> Scope: self-improvement v0 only. This note does not widen mutation authority beyond the existing allowed-now classes.

---

## Current Registry Snapshot

The current bounded registry result is:

- `promote=6`
- `park=1`

Promoted families now cover:

- consumer first-hop packaging
- deliberation latency cueing
- task-board parking clarity
- shared-edit overlap clarity
- publish/push posture clarity
- mutation follow-up routing

Parked family:

- `operator_retrieval_cueing_v1`

So the next candidate should stay inside an allowed-now class while avoiding:

- retrieval mythology
- new transport claims
- governance or identity semantics

---

## Admitted Candidate

`surface_versioning_lineage_clarity_v1`

Target surface:

- `surface_versioning.compatibility_posture`

Why this candidate:

- it sits inside `consumer-parity and readout packaging`
- it directly improves Codex/Claude/dashboard/observer interpretation parity
- it clarifies fallback lineage without touching transport or authority

---

## Baseline Story

`surface_versioning` already exposed:

- runtime surfaces
- consumer shells
- fallback rule
- source-of-truth rule

But it still did not say, in one bounded operator-facing place:

- which shells are repo-native entries
- which shells are bounded adapters
- what exact fallback chain all of them must share

That gap makes cross-consumer lineage readable only if the reader mentally combines multiple fields.

---

## Success Metric

Promote only if:

- the new compatibility posture is present
- it preserves one shared fallback chain
- it keeps Codex CLI as repo-native entry and dashboard/Claude as bounded adapters
- consumer-drift alignment remains green

---

## Rollback Posture

If the trial fails:

- remove the compatibility-posture packaging
- keep prior surface-versioning fields intact
- retain the result only in the dedicated self-improvement status surface

---

## Overclaim To Avoid

Do **not** say this means:

- stronger shared cognition
- vendor-native interop
- better transport semantics
- deeper runtime truth

This is only clearer lineage packaging.

---

## No-Go List

This admission does **not** reopen:

- operator-retrieval runtime
- packet truth order
- observer hierarchy
- session-start transport semantics
- governance or identity layers
