# ToneSoul Sixteenth Trial Candidate Admission (2026-04-07)

> Purpose: admit one sixteenth bounded self-improvement candidate after the fifteenth trial result is visible.
> Status: admitted candidate
> Authority: planning aid only. Runtime truth remains in code, tests, and accepted architecture contracts.

---

## Admitted Candidate

- `candidate_id`: `memory_panel_tier_subordination_v1`
- `target_surface`: `dashboard.memory_panel.reference_boundary`
- `target_consumer`: `dashboard_operator_shell`

## Why This Candidate

The dashboard workspace is now tiered, but the reference panel still has one real failure mode:

- it can look like a second operator-truth region
- it can smooth over closeout pressure with reference abundance
- it still carries display-layer noise that weakens trust

This candidate exists to make the panel's subordination explicit.

## Baseline Story

Current memory-panel behavior is directionally correct, but its boundary language is too soft and its display layer still carries avoidable encoding noise.

## Candidate Story

`dashboard.memory_panel` should expose:

- `reference_boundary_class = auxiliary_only`
- a clear `reference_boundary`
- a `selection_caution` when closeout attention is active
- a cleaner, UTF-8-safe operator-facing view-model

This remains dashboard packaging only.
It must not become:

- retrieval truth
- hot-memory truth
- a second control plane

## Success Metric

`memory_panel_probe.present and consumer_drift_report.status == aligned`

## Failure Mode Watch

- reference selection starts reading like operator truth
- the memory panel begins to compete with Tier 0 / Tier 1
- closeout caution is hidden again behind smoother reference prose

## Rollback Path

Restore the prior memory-panel wording and remove the extra boundary packaging.

## Overclaim To Avoid

Clearer memory-panel packaging is not:

- better retrieval
- better memory transport
- better collaboration outcomes
- shared cognition

## No-Go List

This trial must not:

- widen retrieval scope
- promote selected references into runtime truth
- create a browser-only planning workflow
- turn the memory panel into a second operator console
