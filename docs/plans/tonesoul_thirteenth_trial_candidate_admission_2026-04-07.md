# ToneSoul Thirteenth Trial Candidate Admission (2026-04-07)

> Purpose: admit one thirteenth bounded self-improvement candidate after the twelfth trial result is classified and registered.
> Scope: self-improvement v0 only. This note keeps the candidate inside anti-fake-completion packaging and away from planners, new permissions, governance widening, or transport mythology.

---

## Current Registry Snapshot

The bounded registry now shows:

- `promote=12`
- `park=1`

Promoted families now cover:

- consumer first-hop packaging
- deliberation latency cueing
- task-board parking clarity
- shared-edit overlap clarity
- publish/push posture clarity
- mutation follow-up routing
- surface-versioning lineage clarity
- launch-health trend clarity
- internal-state action clarity
- hook-chain trigger clarity
- consumer misread-guard clarity
- subsystem-parity focus clarity

Parked family:

- `operator_retrieval_cueing_v1`

So the next candidate should:

- stay inside an allowed-now class
- improve successor handling of incomplete work
- reduce one remaining guess that still exists even after closeout visibility is present

---

## Admitted Candidate

`closeout_attention_action_clarity_v1`

Target surface:

- `closeout_attention.operator_action`

Why this candidate:

- `closeout_attention` already lifted incomplete handoffs into first-hop shells
- successors still had to infer:
  - which family generated that attention
  - what concrete pressures were active
  - what bounded operator action followed from a partial or blocked closeout
- the candidate stays inside anti-fake-completion packaging only and directly targets cross-shell successor handling

---

## Baseline Story

`closeout_attention` already exposed:

- `present`
- `status`
- `summary_text`
- `receiver_rule`

But it still left one operator burden:

- infer whether the attention came from bounded-handoff closeout or some other review lane
- reconstruct whether unresolved items, stop reason, or human gating made the warning current
- guess the next bounded action instead of reading it directly

That meant the warning existed, but the action grammar was still thinner than the visibility grammar.

---

## Success Metric

Promote only if:

- `closeout_attention` exposes `source_family`
- it exposes `attention_pressures`
- it exposes `operator_action`
- dashboard Tier-1 shell preserves the same `source_family` and `operator_action`
- Claude-style adapter preserves the same `source_family` and `operator_action`
- the change adds no planner story, no new permissions, and no stronger authority claim

---

## Rollback Posture

If the trial fails:

- remove the extra closeout rationale packaging
- fall back to `status + summary_text + receiver_rule`
- keep the lesson only in the dedicated self-improvement status surface

---

## Overclaim To Avoid

Do **not** say this means:

- stronger mutation authority
- autonomous planning
- solved collaboration safety
- stronger governance truth

This is only clearer anti-fake-completion handling.

---

## No-Go List

This admission does **not** reopen:

- planner or scheduler behavior
- new hook families
- governance or identity layers
- retrieval/runtime truth mixing
- transport semantics
