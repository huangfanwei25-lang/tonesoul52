# ToneSoul Beta Wave Operator Note Template (2026-04-28)

> Use this template for `docs/status/beta_wave_2026-04-28_operator_<label>.md`.
> Fill it from the operator's actual run. Do not promote interpretation into fact.

---

# ToneSoul Beta Operator Note (`<label>`)

> Task shape: `A / B / C`
> Session id: `<stable local id>`
> Participant label: `<public-safe label>`

## 1. Session Setup

- participant type:
- surface used:
- operator present:
- manual steering allowed:

## 2. System Path

- verdict path: `approve / refine / block / n/a`
- notable council/governance behavior:
- did the system remain bounded to the requested task:

## 3. Evidence Row

- `session_id`:
- `task_shape`:
- `participant_type`:
- `surface_used`:
- `system_verdict_path`:
- `task_outcome`:
- `trust_before`:
- `trust_after`:
- `trust_note`:
- `manual_rescue_required`:
- `governance_friction`:
- `claim_issue`:
- `detector_issue`:
- `next_fix_candidate`:

## 4. Operator Read

- what worked:
- what looked product-level:
- what looked governance-level:
- what looked docs/entry-level:

## 5. No-Count Checks

- was the participant too warm/context-rich:
- did the operator over-steer:
- did the session drift out of the bounded task shape:
- if yes to any, why this session should be downgraded:

## 6. Follow-Through

- should this session feed:
  - beta evidence summary
  - `strategy_mirror` calibration
  - entry-surface cleanup
  - claim-honesty rewrite
- one smallest next action:
