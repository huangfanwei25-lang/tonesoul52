# /grill 決策樹記錄 — 資源接回(2026-07-06,目標模式)

> /grill 深拷問資源接回設計。**收斂結論:這批只做 A(尾聲點名資源代價)+ R1(修 resume NaN);
> B(能源閘第三路)凍結**——五視角一致抓到 B 會讓能源間接決定結局家族(囤能源→岔軌者),
> 違反〇原則。C 裝飾、不綁。全程同模型;三點交 codex 跨模型驗(見末節)。五視角原稿存 §附錄。

---

# 【Grill 決策樹記錄】資源三軸接回劇場 — 五視角綜合

> 五視角(Guardian / Analyst / Critic / Axiomatic / Advocate,皆同 claude 分身)已逐條讀碼標 E 級。本記錄織成決策樹,交 owner 拍板,並附我動手前清單。
> 涉及檔案:`site/theater/app.js`、`site/theater/gamedata/events.json`
> 三接點定義:**A** = 尾聲具名後果 readout;**B** = 能源閘門(卡第三路);**C** = 動態風險警告

---

## 一、決策樹:每個關鍵問題的歸類

### 【可現在解】— 有明確答案,我動手前先做掉

| # | 問題 | 答案 | E 級 | 理由 |
|---|------|------|------|------|
| R1 | **resume 讀舊檔會 NaN/TypeError** | resume 改為 merge 預設:`resources:{...{medical:5,energy:5,trust:5},...(prior.resources||{})}`(app.js:240-245) | **E4** | 今天的 NaN 判例根因。舊檔缺軸→`S.resources[k]` undefined→HUD(:380)整包炸、A 尾聲(:1033)印「信任 undefined」。這是**接回任何一點的前置**,不做就必壞。 |
| R2 | **「低」的定義三處不一** | 抽單一常數,HUD/A/C 共用(值本身見 O-A2) | **E4** | 現況 HUD≤2(:381)、A 擬 ≤3/≥7、B 擬 <4 = 語義分裂。統一「機制」是可現在解;統一「數字」牽品味,拆到 owner。 |
| R3 | **A 門檻是否給機械好處** | **純點名、零 reward**。結局家族(:127-140)不讀 resources,A 不得重新耦合 | **E3** | restraint:漂亮 readout 本身就是訊號,再給機械回饋=把 min-max 目標明示。守「記錄非評分」。 |
| R4 | **9 個第三路各在哪些 event** | 枚舉 events.json(`is_third_path:true` = 9 個,`third_path_cost` = 9 個一一對應) | E2 | Analyst 已數出;B 若做,要先知道能源閘實際卡住哪幾局(靜態掃)。 |
| R5 | **第三路一律走 flag 非硬編** | 用 `opt.is_third_path`(:605/:623 已是),**禁止**硬編「C」 | **E4** | E01/E03/E04/E11 無第三路、E10 第三路是 B 不是 C。硬編 letter 會漏站。 |

### 【owner 決定】— 品味 / canon,我攤清單不代拍

**接點 A(尾聲具名 readout)**
- **O-A1｜中段 4-6 印什麼?** 起始 5、deltas 小,醫療/信任常落 4-6;≤3/≥7 兩端才觸發→中段空白會**看似壞掉**。→ 給中性句 or 留白?
- **O-A2｜門檻數字 + register。** ≤3/≥7 的具體門檻值(牽動 R2 統一);且 ≥7 端**是否也點代價、去褒獎詞**(Axiomatic:≤3 配 ≥7 成對=靠對稱暗示善惡,違「不判善惡」)。確切字=品味。

**接點 B(能源閘)— 本輪最脆,四視角同標 E4 反對**
- **O-B1｜B 到底做不做?** 五視角一致警告:B 會產生**囤能源→解鎖晚期第三路→湊滿 `thirds>=3`→拿「岔軌者」結局**的路徑(classifyEndingFamily:135),等於**能源間接決定結局家族**,直接違反〇原則「資源不碰道德形狀」。Critic/Advocate 傾向**不做 B、只留 A(+C)**。
- **O-B2｜若做:關閉 vs 加價?** 五視角一致:**別關閉**(關閉=在能源枯竭者臉上塞回二選一,打臉第六律;且 E08-C 第三路本身 +1 能源,關掉=堵死唯一回血)。若加價,**用 `third_path_cost` 既有的 failure_cost/extra_sacrifice/future_debt 具名非能源代價**,**不要用能源計價**(clamp 0-10 下低能源時代價 floor 到 0 = 假價,Critic 4)。
- **O-B3｜thirds≥3 家族閘 × 能源閘的耦合怎麼斷?**(改的是判定語義)選項:(a) B 只擋 HUD/敘事表現、**不擋 thirds 計數**;(b) 家族改讀「第三路是否付了六欄代價」而非**次數**。
- **O-B4｜哪些第三路「需電力」?** events.json **現無此欄位**。要嘛全套 9 個(更違原則)、要嘛新增欄位(=內容工,非順手)。

**接點 C(動態警告)**
- **O-C1｜C 做不做 + 門檻 + 去重規則?** Advocate/Critic 標為**裝飾**(display-only、無分支、events.json 無條件警告欄→只能硬編通用句)。能源全域淨負、後段常態 ≤3,若「軸低就跳」→中後期每站跳=橫幅盲視。建議:改「本選項 `resource_effects[軸]<0` **且**該軸已低」才警告,綁具體傷害而非全域狀態。

### 【動手時驗】— 實作必驗,寫完要跑過才算數

| V | 驗什麼 | 出處 |
|---|--------|------|
| V1 | 舊存檔(resources 既有/缺軸)讀回**不 NaN**;`bumpResource` 對 undefined 不炸 | Guardian/Analyst/Advocate |
| V2 | HUD bar(:380)/ 預覽 / 尾聲 readout(:1033)**三處資源語義一致** | Critic/Axiomatic/Advocate |
| V3 | A 尾聲**別與 :1033 既有裸數字重印成第二處**(否則設計 §三.6「顯示殘留」) | Axiomatic/Advocate |
| V4 | 修 stale 註解:app.js:100「城亮=能源≥5」已過期(:127 早改讀 evaded>owned),別讓耦合復辟 | Critic Q5 |
| V5 | 若做 B:跑「連吃所有第三路」腳本確認**不會把玩家鎖出 switcher(thirds≥3)**;跑「屯能源」腳本確認**沒有乾淨最優路** | Critic/Axiomatic/Advocate |
| V6 | A 每個門檻(醫療/能源 ≤3、≥7)在合理玩法下**可達且可避**;**不可避的資源不進 readout**(否則是誣賴,非問責) | Critic Q4 |

---

## 二、最小先做 vs 三接點全做(據 Advocate)

| | 最小先做 | 三接點全做 |
|---|---------|-----------|
| **範圍** | **只做 A 具名 prose** | A 尾聲 + `renderOptions`(:600)第三路 + `risk_disclosure` + events.json 加欄位 + `classifyEndingFamily` 交互 |
| **動幾處** | 一處(:1033 尾聲段)+ R1 前置 | 四處 surface |
| **碰結局家族線?** | **否**(A 只換裸數字為具名 prose,讀既有 `S.resources`,零新狀態) | **是**(B 踩 thirds≥3 家族閘) |
| **存檔 schema** | 不動(save 已存 resources) | B 加欄位=改內容資料 |
| **原則風險** | 低(資源仍純物質質地、不決定輸贏,反而更貼 measure-don't-inflate) | 高(B 把資源偷渡回道德軸) |
| **建議** | ✅ **這批只做 A** | ⏸ B 拆成獨立設計、先解 O-B3 再談;C 併不併看 O-C1 |

**先後對照:**R1(補預設,前置)→ A(答完 O-A1/O-A2 即可動)→ 觀察真人反應 → 再評 B/C。A 幾乎已完成 80%(:1033 早已印資源數字),CP 值最高、風險最低。

---

## 三、收斂判定

**A 接點:接近收斂。** 只欠 O-A1(中段印什麼)、O-A2(門檻值 + register)兩個品味決定,加 R1/R3/V6 我可自理。→ **owner 答完 O-A1/O-A2 即可動 A。**

**B 接點:未收斂,有 4 條懸空分支。以下沒答完不准動手 B:**
1. **O-B1** — B 做不做(五視角傾向不做)
2. **O-B2** — 關閉 vs 加價(且加價禁用能源計價)
3. **O-B3** — thirds≥3 × 能源閘耦合怎麼斷 ← **最硬的閘,無技術共識,純 owner 語義決定**
4. **O-B4** — 哪些第三路需電力(現無欄位)

> 特別標記:**O-B3 未決 = B 一律凍結。** 這是唯一會「把資源偷渡回結局家族、違〇原則」的分支,五視角同標 E4。在 owner 明確裁定耦合斷法前,不得碰 `renderOptions` 第三路渲染或 `classifyEndingFamily`。

**C 接點:未收斂但低優先。** O-C1 未答;且 C 是裝飾、若與 A 語義沒對齊反造成「HUD 喊低、結果沒事」不一致。**不值得和 A 綁一批做。**

---

## 四、誠實收尾

**這輪五視角(全同 claude)抓不到什麼:**
- **手感 / 樂趣。** 具名 prose 到底讀起來像「問責」還是「雜訊」,五個分身都在扶手椅上推,**只有真人試玩能定**。
- **min-max 是否真實可達。** 五視角**一致斷言** B 會生「囤能源→岔軌者」min-max 路(全引 classifyEndingFamily:135),**但沒有任何一個實際跑過模擬**——這是同源 correlated blind spot:斷言 E4,證據其實是 armchair。可能理論存在但實際玩不出來,也可能更糟。
- **register 是否讀成評判。** ≤3 prose 會不會被讀成「遊戲怪我」,尤其非設計者的真人讀者,同模型自評不可信。

**明確交 codex 跨模型審的 3 點(owner 要 codex 審,我會照這清單去問):**

1. **classifyEndingFamily thirds≥3 × B 能源閘的耦合是否真實可達。** 請 codex **獨立枚舉** events.json 的能源經濟(起始 5、正來源僅 E01A/E08A/E08C/E12E,其餘全扣)+ 9 個第三路的能源成本分布,實跑/窮舉確認「囤能源→解鎖晚期第三路→湊滿 thirds≥3→岔軌者」這條 min-max 路**是否真的玩得出來**,還是理論存在、實際被能源耗竭自我封死。五視角同源斷言 E4,此點最需外眼驗真偽。

2. **resume/save 的 NaN 判例根因是否只補預設就夠。** 請 codex 獨立讀 save/load path(app.js:178-191、240-245)+ `bumpResource` clamp(:740),確認 merge 預設(R1)是否封住**所有**缺軸/型別路徑,還有沒有其他會炸的入口(例如 clamp 對 undefined、部分軸缺失)。

3. **能源計價在 clamp 0-10 邊界的真實性。** 若 owner 選 O-B2 加價,請 codex 驗:任何以能源計價的代價在 energy≤1 時是否 floor 到 0 = 形同免費(Critic 4),據此裁定「加價一律走非能源具名代價」是否為硬約束。

---

# 附錄 · 五視角原始拷問



## 視角 1

Guardian 拷問完成。逐條讀碼標 E 級如下(繁中,≤650 字)。

---

**Guardian:資源接回會怎麼壞**

1. **存檔:resume 不補預設會炸(E4,最高危)** `initGate` 的 resume(app.js:240-245)是 `Object.assign(S, {resources: prior.resources...})`——**直接覆蓋、不 merge 預設**。舊檔若缺 `resources` 或缺某軸,`S.resources` 變 undefined/漏軸:renderChapter HUD(:380 `S.resources[k]`)整包 undefined 時直接丟 TypeError;接點 A 尾聲(:1033)印「信任undefined」。這正是今天的 NaN 判例。**可現在解**:resume 改 `resources:{...{medical:5,energy:5,trust:5},...(prior.resources||{})}`。

2. **接點 B:不是每站都有第三路(E4)** E01/E03/E04/E11 **無** is_third_path;E10 第三路是 **B 不是 C**。若閘門碼硬編「C」會漏 E10、且 4 站根本無路可閘。設計「能源掉越多越沒第三路」是假象——那 4 站與能源無關。**動手時驗**:一律走 `opt.is_third_path` flag(現碼 :605/:623 已是),加 regression。

3. **接點 B:閘=關閉會逼二選一,自打第六律(E3)** 關第三路=強迫「拒絕二選一」的玩家回到二選一,直接反 opt 卡上寫的「第六律」。且 E08-C 第三路本身 **+1 能源**,低能源時關掉它=堵死唯一回血。**owner 決定**:關閉 vs 加價——Guardian 建議**加價(具名代價)永不關閉**。沉默鍵(:643)永遠在,不會硬卡敘事,但簽名機制會在高壓時被抽走。

4. **接點 B:製造它自己禁止的 min-max(E3,restraint)** 「能源<4 鎖第三路」讓能源變成必須囤積以保留選項的最優量——正是原則§〇要拒的可最佳化資源。**owner/thesis 決定**:代價要不可預測且具名,否則被算穿。

5. **接點 A:中段 4-6 印什麼?(E3)** 起始 5、deltas 小,醫療/信任常落 4-6;≤3/≥7 兩端都不觸發→尾聲那段空白=看似壞掉。**owner 決定**:中段給中性句還是留白(:1033 已無條件印原始數字,A 是疊敘事)。

6. **接點 C:門檻與雜訊(E3)** 能源全域淨 -13、後段常態 ≤3,若警告=「軸低就跳」→中後期每站跳=橫幅盲視。**動手時驗**:改「本選項 resource_effects[軸]<0 **且** 該軸已低」才警告,綁具體傷害非全域狀態。

7. **「低」的定義三處不一(E4,設計自標#6)** HUD 用 ≤2(:381)、A 擬用 ≤3/≥7、C 待定——三套門檻=語義分裂。**可現在解**:抽單一常數,三處共用。

**動手前必答**:B 關閉還是加價(owner);A 中段印什麼(owner);C 門檻+去重規則(owner);resume 補預設(可現在解,先做);第三路一律走 flag 非硬編(動手時驗)。


## 視角 2

以下為 Analyst(機制/證據)拷問,每點標 E0-E4。

**1. 第三路現況結構 [E4]** `renderOptions` 迴圈中 `opt.is_third_path` 只驅動 CSS class + 標籤;`third_path_cost` 是**純文字**物件(6 欄:requires_help_from / success_condition / failure_cost / extra_sacrifice / breaks_rules / future_debt),**無任何數值成本欄、無能源欄**。渲染在 `is_third_path && third_path_cost` 時無條件展開 `<details>`。→ B 要插「能源不足」邏輯:迴圈內能讀全域 `S.resources.energy`,**結構插得進**(加分支即可);但「雙倍代價/額外具名代價」在資料層**不存在**——只能複用 `extra_sacrifice` 散文或新增欄位。「雙倍」無數字可乘。

**2. 影響幾個選項 [E4]** `is_third_path:true` = **9 個**,`third_path_cost` = **9 個**(一一對應)。能源閘門最多影響這 9 個選項。各屬哪些 event、能源起始/門檻交互曲線需真人試玩(設計點 3)。

**3. 能源閘門與現有 low 閾值衝突 [E4]** HUD 已用 `S.resources[k] <= 2` 標 low(L381);設計 A 用 ≤3/≥7、B 用 <4。**三套閾值不一致** → 設計點 6「語義一致」已是現存地雷。

**4. A 具名後果來源 [E4]** 結局三問 L1033 **已直接印原始數字**(`醫療X/能源Y/信任Z`)。散文具名後果(「白線醫療站數次拒收」)**無可讀來源**——events.json 無「資源門檻→具名後果」欄,`harm_targets` 是逐選項的、已由 `allHarms()` 呈現。A 的門檻散文=**全新硬編字串表**(keyed by 資源等級),寫在 app.js。restraint 警訊:數字已可見,再疊「≥7 好句」=**把 min-max 目標明示**。

**5. C 動態警告 [E4]** `risk_disclosure` 是**靜態物件**,渲染時 `Object.entries` 迭代 + `RISK_NAMES` 對映(L617-619)。資料層無「資源條件警告」欄。動態注入=渲染側在迴圈後**條件 append 一列 `<tr>`**(可讀 `S.resources`),**結構支援**,但屬 render-side 新邏輯,非讀既有欄。

**6. 存檔 [E4/E2]** `save()` 已存 `resources`,A/B/C 讀既有 → **無 schema 改動、舊檔相容**。但 `bumpResource` 用 clamp,若某 key 缺失會 NaN(設計點 5);S.resources 三 key 初始化需**動手時驗**。

---
**動手前必須先答:**
- B「關閉 vs 加價」+ 加價用散文或新數值欄?**[owner 決定]**(牽動品味+是否動內容工)
- 三套資源閾值(HUD≤2 / A≤3,≥7 / B<4)要否統一?**[owner 決定]**
- A 具名後果字串表:每資源 3 段硬編句,誰寫(內容)?**[owner 決定]**
- 能源起始值(設計稱 5)與門檻 4 的實際曲線 **[動手時驗:讀 S.resources 初始化 + 跑一局]**
- S.resources 三 key 是否恆存在(防 NaN)**[可現在解:讀 init]**
- 9 個第三路各在哪些 event、B 門檻實際卡住幾局 **[可現在解:枚舉 events.json]**

代碼位置:`site/theater/app.js` renderOptions L600-662、結局三問 L1031-1054、save L178-191、allHarms L980;`gamedata/events.json` third_path_cost 樣本 L438-445。


## 視角 3

Critic 攻擊(claim ≤ evidence,每點標 E 級;已讀 app.js + events.json 全部 resource_effects):

**接點 B 是全設計最脆處**

1. **B 造出 min-max 正解,直接違反〇原則。**(E4) 能源起始 5、正能源來源只有 E01A+1/E08A+2/E08C+1/E12E+1,其餘幾乎全扣→經濟淨負、單調下降。門檻 4 的意思就是「早期別花能源、屯到後段砸第三路」。想吃 switcher 結局(thirds≥3)的玩家會學到囤能源。這正是 restraint 要拒的可算出最優路。

2. **B 懲罰的恰恰是走第三路/人道的玩家(自噬迴圈)。**(E4) 第三路本身燒能源:E02C −2、E06D −2、E06C/E07C/E09C/E12C −1。連吃第三路→能源跌破門檻→**後面的第三路被閘掉**。追 switcher 結局的人反而被資源耗竭鎖死那條結局家族。這是跨系統隱形難度牆,和「不判善惡」矛盾:它用管資源能力篩掉了道德姿態。

3. **關閉 vs 加價:兩個都壞,但關閉更壞。**(E3) 關閉=在能源枯竭的人臉上強行塞回二選一,直接打臉第六律(拒絕二選一)。加價較一致。但——

4. **加價用能源計價在低能源時是假的。**(E4) bumpResource 夾在 0–10(app.js:740),能源=1 時「雙倍能源代價」floor 到 0,價碼沒被感受到。若要加價,得用 third_path_cost 既有的 failure_cost/extra_sacrifice/future_debt 具名非能源代價,別再堆能源。

**接點 A**

5. **A 名義上「只點名」擋不住最佳化,但傷害輕。**(E3) 敘事/漂亮 readout 本身就是獎勵訊號;≤3/≥7 是偽裝成三桶的分數。所幸結局家族讀 owned/evaded+brokeFaith、**不讀資源**(app.js:127),兩軸解耦→刷 readout 不影響道德結局,無機械回饋,危害有限。

6. **但 A 的門檻可能點名玩家無法避免的代價=誣賴。**(E4) 醫療正來源只有 E06A+1、多個 −1;能源同樣難拉到 ≥7。所以 readout 幾乎必然顯示「醫療數次拒收」,不論玩家怎麼選。這不是「點名你造成的物質代價」,是點名結構性必然→讀起來像遊戲無條件怪你,反而弱化問責框架。

**方向本身**

7. **「該不該硬接回」——誠實收掉是真的選項,標 owner 決定。**(E2) 現況 resources 只餵 HUD/預覽/下載。A 本質是「誠實承認非決定性的裝飾點名」,B 才是真接回但如上會生 min-max。若 B 拿掉,剩 A+C 幾乎等於「明說資源是物質質地、不決定輸贏」——那其實比硬接更誠實、也更貼 measure-don't-inflate。是否值得為 B 的一刀承擔 min-max 風險,是品味題。

**動手前必須先答**

- Q1(**owner 決定**)B 到底做不做?若 min-max(囤能源砸晚期第三路)無法用非能源計價消掉,restraint 傾向不做 B、只留 A+C。
- Q2(**owner 決定**)關閉 vs 加價 → 建議加價,且**用具名非能源代價**,不用能源(否則第 4 點假價)。
- Q3(**動手時驗**)跑一次「連吃所有第三路」腳本,確認不會把玩家鎖出 switcher 結局(thirds≥3);跑「屯能源」腳本確認沒有乾淨最優路。
- Q4(**可現在解**)A 上線前,先算每個門檻(醫療/能源 ≤3、≥7)在合理玩法下**可達且可避**;不可避的資源不要進 readout,否則是誣賴。
- Q5(**動手時驗**)別重新引入 energy≥5 耦合:app.js:100 註解「城亮=能源≥5」已是 stale(碼在 :127 早改讀 evaded>owned)。接 B 時順手修這條 stale 註解,別讓它復辟。
- Q6(**動手時驗**)HUD bar / 預覽 / 尾聲 readout 三處資源語義一致;舊存檔(resources 既有)讀回不 NaN。

證據檔:`site/theater/app.js`(:68 起始、:116 endingAxes、:127 sank、:600 renderOptions、:623 third_path_cost、:740 clamp、:1033 三問 readout)、`site/theater/gamedata/events.json`(全 resource_effects 已列)。


## 視角 4

讀完設計全文、app.js(結局判定 96–140、第三路渲染 600–662、三問尾聲 1001–1054)、events.json(12 事件/9 第三路)、天道 canon(整合 v1)。以 Axiomatic 視角(原則一致性)拷問:

**檢查 1|restraint:A 門檻 + B 能源閘會不會合成「顧資源=好結局」隱性最優路?**
- 【E4】結局家族 `classifyEndingFamily`(app.js 132–140)只讀 silences/thirds/sank/brokeFaith,**完全不讀 resources**——MVP 已把 energy 剔除(註 100/110)。所以資源本身動不了道德形狀。這一半是安全的。
- 【E4—最尖的地雷】但 `thirds>=3 → "岔軌者"`(line 135)。B 讓能源 ≥ 門檻才開第三路;events.json 有 4 個選項 `energy` 正 effect(可養能源)。**合起來 = 「囤能源 → 保持第三路可用 → 湊滿 thirds≥3 → 拿到岔軌者家族」**——一條 resources→結局家族 的可 min-max 路,經由第三路計數器繞過「資源不進判定」的防線。這正是 MVP 想拆掉的評分感,從側門回來。**這是本輪最該先解的。**

**檢查 2|不判善惡:A 的 ≤3 措辭會被讀成「你做錯了」嗎?**
- 【E4】天道終句(canon 235–238)=「保存後果、不審判、禁止讓犧牲者無名」;第一律「只記錄選擇造成了什麼」。「白線醫療站數次拒收」是**記錄事實**,合規。
- 【E3】但 ≤3 配 ≥7(「大多接住了」)成對出現=靠對稱暗示善/惡極。要守「點名非評判」:≥7 端也點**它的代價**(能源/信任是從哪裡挪來的),避免褒獎詞;register 對齊 line 1027 既有的「不評善惡+純列事實」。**確切字=owner 品味。**

**檢查 3|B 能源閘 vs 第六律(拒絕二選一≠拒絕代價)一致嗎?額外一層還是重複?**
- 【E4】第三路**已**由 canon 其六(128–132)強制六欄代價,app.js 623–632 全渲染。代價已經收足。
- 【E2】B 若做成「能源<門檻→雙倍代價」=在 canon 已完整定價的路上**再疊一筆資源價=重複收費**,與第六律語義撞車。若做成「電力不足→關閉/降級」=**可用性前提**(火花需電=物質質地),與「代價」不同層、乾淨。**建議走可用性,不走加價。**

---
**動手前必須先答:**
1. thirds≥3 家族閘 × B 能源閘的耦合怎麼斷?(選項:B 只擋「敘事表現/HUD」不擋 thirds 計數;或家族改讀「第三路是否付了六欄代價」而非次數)— **owner 決定**(改的是判定語義)。
2. B = 關閉 vs 加價?依檢查 3 建議關閉/降級 — **owner 決定**(手感)。
3. A 的 ≥7 端是否也點代價、去褒獎詞?— **可現在解**(寫 spec 時定 register)。
4. 養能源選項(4 個 +energy)在 B 上線後會不會變「該優先點」?— **動手時驗**(真人試玩前先靜態掃 min-max 路徑)。
5. 三問「kept」行(line 1033)**已印**資源數字;A 別在尾聲重印成第二處語義,否則 §三.6 顯示殘留 — **動手時驗**。


## 視角 5

讀完設計全文 + app.js 關鍵段 + events.json,以下 Advocate 逼問(每點標 E 級):

**一、CP 值排序(只做一個先做 A,確認)**
- A 幾乎已完成 80%:app.js:1033 尾聲**已經**印「城市狀態 醫療X/能源Y/信任Z」。所謂「只餵 HUD/預覽/下載」不準——資源早進尾聲了。A 只是把裸數字換成具名 prose(≤3/≥7 句),讀既有 `S.resources`、零新狀態、不碰結局家族/第三路/風險揭示。**最低風險、最高 CP,先做 A 正確**。(E4)
- B CP 看似高、實際最貴且**踩自己的原則**(見二)。C 是裝飾(見三)。

**二、B「一刀解兩題」不成立,反而更慘(最強反對點)**
- `classifyEndingFamily()`(app.js:135)**第一條**就是 `thirds>=3 → switcher`。第三路數量**直接決定結局家族**。B 用能源 gate 第三路 = 能源**間接決定結局家族** = 直接違反〇原則「資源不碰結局家族/道德形狀留錨鏈」。這不是順手解,是把資源偷渡回道德軸。(E4)
- events.json 有 **9 個** third_path,**無「需電力」欄位**。B 講「火花需電力」但資料上分不出哪條需電力 → 要嘛全套(更違原則)、要嘛加欄位(=內容工,非順手)。(E4)
- Spiral:起始 5、門檻 4,而第三路本身常扣能源(E05 第三路區 energy −2)。玩家為救人燒能源→掉到 3→鎖第三路。你的抱怨從「能源只扣」變成**「能源只扣還害我沒出路」——確實更慘**。(E3)
- min-max:能源≥4 解鎖 = 能源變「要囤的分數」= 原則明禁的可最優化路。設計三.2 自己標了,Advocate 確認這是 B 的致命傷。(E3)

**三、C 輕版=裝飾,無約束**
display-only 警告、無分支、events.json 無條件警告欄位 → 只能硬編通用句。「看得見的約束」但不改任何結果 = 純 telegraph。有一點預期管理價值,但若和 A 的尾聲語義沒對齊(設計三.6 自標),反而製造「HUD 喊低、結果沒事」的不一致。**不值得和 A 綁一批做**。(E3)

**最小先做 vs 全做**
- 最小:**只做 A 具名 prose**。動一處(app.js:1033 尾聲段),不動第三路渲染、不動風險揭示、不動 events.json、不動存檔 schema。驗「資源被記名,玩家有沒有感」。
- 全做:動尾聲+`renderOptions`(600)第三路+`risk_disclosure`+events.json 加欄位+`classifyEndingFamily` 交互——四處 surface、踩結局家族線。**先別**。

**動手前必須先答**
1. B 是否接受「gate 第三路=間接改結局家族,違〇原則」?→ **owner 決定**(我方建議:B 只做「加價/具名代價」不做「關閉」,且**不**讓能源低到擋掉第 3 條 thirds,否則污染 switcher 判定)。
2. 哪些第三路「需電力」?events.json 現無欄位。→ **owner 決定**(加欄位=內容工,非順手)。
3. A 的 ≤3/≥7 prose 是否給任何機械好處?→ **可現在解**:純點名、零 reward,守 restraint。
4. 舊存檔 `resources` 讀出為 undefined/NaN 時尾聲 prose 的 fallback?→ **動手時驗**(今日 NaN 判例)。
5. A 尾聲、HUD(:380 low 標記)、C 警告三處語義一致?→ **動手時驗**。

建議:**這批只做 A**;B 拆成獨立設計、先解決「第三路 gate 是否碰 switcher」再談。
