# 劇場水墨渲染層 — 工單(2026-07-06,owner 提案)

> Status: DRAFT,owner 已提案(「充滿張力的黑白城市水墨畫,和能源/信任/故事進展聯動」);
> 本 session 剩量不足不開刀,下個 session 帶新鮮上下文執行。
> 紀律:渲染層(佈景可幻想)——**不改任何物理/判定**;純前端,零伺服器、零外部資產
> (授權乾淨:不借用任何美術,程序化生成)。

## Framing

每章/每個選擇配一幅程序化黑白水墨(SVG:feTurbulence 墨暈、城市剪影、留白構圖),
由**真實遊戲狀態**驅動——畫就是儀表板,渲染層與誠實物理同構。

## 聯動表(v0)

- 信任 → 墨緣收斂度(低=毛散暈開,高=輪廓定)
- 能源 → 明暗/留白比(低=濃黑、天際線熄燈)
- pressure_level → 筆觸抖動與傾斜
- 章節 index + 撤回碼 → **決定論種子**:同軌痕同畫,回讀重現當時的城;
  畫成為紀錄的一部分(別人重放你的軌痕=看見你看過的城)
- 重大轉轍(第三路/沉默)→ 構圖突變(owner 拍:要不要唯一一點硃砂印作 accent)

## Scope

- `site/theater/ink.js`(新,純函式:state+seed → SVG 字串)+ app.js 各章 header 掛一次
- CSS 不擋既有 UI;`prefers-reduced-motion` 尊重
- 驗收:node --check 綠;同 seed 重繪 byte-identical;關 JS 畫面仍可玩(漸進增強);
  線上 curl 驗到 SVG 出現

## 增補:小說資料夾人物圖(owner 同日追問「可以用嗎?」)

可以,**前提=出處是 owner 自產**(自繪或 owner 自己生成;若有任何外來素材則不上公開站)。
桌面 `小說\` 有 24 個圖檔(NPC 立繪+介面截圖,收斂掃描已記)。用法與水墨層分工:
- **NPC 立繪** → 對話框頭像層(誰說話露誰;衡樞追問時=衡樞的臉,張力直接翻倍)
- **程序化水墨** → 章節/選擇的背景層(狀態驅動,見聯動表)
- 兩層並存不打架:立繪=人,水墨=城
- 工程:圖檔壓縮(webp,單檔 <150KB)收進 `site/theater/assets/`;首次收檔 commit 註明
  出處=owner 自產;lazy-load 不拖慢首屏
- **執行前先問 owner 一句:這 24 檔全部是你自產嗎?**(授權乾淨是上站的硬條件)

## Lane

下個 Claude session 實作(視覺品味迭代要 owner 看圖回饋 2-3 輪);merge 前 owner 過目。
