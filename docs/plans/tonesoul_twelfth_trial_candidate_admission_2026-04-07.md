# ToneSoul Twelfth Trial Candidate Admission (2026-04-07)

> Purpose: admit one twelfth bounded self-improvement candidate after the eleventh trial result is classified and registered.
> Scope: self-improvement v0 only. This note keeps the candidate inside successor-facing packaging and away from planners, new permissions, governance widening, or transport mythology.

---

## Current Registry Snapshot

The bounded registry now shows:

- `promote=11`
- `park=1`

Promoted families now cover:

- consumer first-hop packaging
- deliberation latency cueing
- task-board parking clarity
- shared-edit overlap clarity
- publish/push posture clarity
- mutation follow-up routing
- surface-versioning lineage clarity
- launch-health trend clarity
- internal-state action clarity
- hook-chain trigger clarity
- consumer misread-guard clarity

Parked family:

- `operator_retrieval_cueing_v1`

So the next candidate should:

- stay inside an allowed-now class
- improve successor orientation across Codex-style, Claude-style, and dashboard shells
- reduce one remaining guess that still exists even after first-hop order, misread correction, and hook recommendation are aligned

---

## Admitted Candidate

`subsystem_parity_focus_clarity_v1`

Target surface:

- `subsystem_parity.next_focus`

Why this candidate:

- `subsystem_parity` already exposed family status and one `next_focus` target
- successors still had to infer:
  - which subsystem family produced that focus
  - what current pressures made it the shortest board
  - what bounded operator action followed from it
- the candidate stays inside successor packaging only and directly targets Codex/Claude/dashboard orientation parity

---

## Baseline Story

`subsystem_parity` already exposed:

- `families`
- `counts`
- `receiver_rule`
- `next_focus.target`
- `next_focus.reason`

But it still left one operator burden:

- infer whether the focus came from mutation hooks, launch posture, or another family
- reconstruct what pressure made that focus current
- guess what the next bounded move actually was without reopening mutation or shell prose

That meant parity existed, but the "why now" grammar was thinner than the maturity grammar.

---

## Success Metric

Promote only if:

- `subsystem_parity.next_focus` exposes `source_family`
- it exposes `focus_pressures`
- it exposes `operator_action`
- dashboard Tier-1 shell preserves the same `source_family`
- Claude-style adapter preserves the same `source_family`
- the change adds no planner story, no new permissions, and no stronger authority claim

---

## Rollback Posture

If the trial fails:

- remove the extra `next_focus` rationale packaging
- fall back to `target + reason`
- keep the lesson only in the dedicated self-improvement status surface

---

## Overclaim To Avoid

Do **not** say this means:

- better planning quality
- autonomous planning
- stronger mutation authority
- stronger cross-agent cognition

This is only clearer successor-facing focus packaging.

---

## No-Go List

This admission does **not** reopen:

- planner or scheduler behavior
- new hook families
- governance or identity layers
- retrieval/runtime truth mixing
- transport semantics
