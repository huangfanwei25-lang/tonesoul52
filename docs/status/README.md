# Status Artifacts

This folder is the repository status source for machine-generated governance snapshots.

## How To Read This Folder

- Read markdown with UTF-8 in PowerShell:
  - `Get-Content -Raw -Encoding UTF8 docs/status/<file>.md`
- Treat `*_latest.json` and `*_latest.md` as generated point-in-time snapshots.
- Do not hand-edit generated `*_latest.*` artifacts unless you are fixing the generator itself.
- Separate machine snapshots from hand-authored reconciliation docs:
  - Generated operational snapshots:
    - `repo_healthcheck_latest.*`
    - `git_hygiene_latest.*`
    - `dual_track_boundary_latest.*`
    - `memory_*_latest.*`
    - `friction_shadow_*_latest.*`
    - `philosophical_reflection_latest.*`
  - Hand-authored historical / handoff docs:
    - `professional_ai_architecture_review_2026-03-04.md`
    - `professional_ai_architecture_positioning_2026-03-07.md`
    - `round2_progress.md`
    - `round2_handoff_framework.md`
    - `orphan_modules_report.md`
- If a generated snapshot conflicts with a historical review, prefer:
  1. commit/file evidence,
  2. generated latest snapshot for current state,
  3. hand-authored review for historical interpretation.

## Human Docs With Special Semantics

- `professional_ai_architecture_review_2026-03-04.md`
  - A historical architecture review, not a current workspace health report.
- `professional_ai_architecture_positioning_2026-03-07.md`
  - An architecture positioning memo, not a test/CI artifact.
- `round2_progress.md`
  - Reconciled handoff summary for the Round 2 orphan-module cleanup.
- `round2_handoff_framework.md`
  - Operator-facing procedure for re-reading Round 2 without mixing history and current drift.
- `orphan_modules_report.md`
  - Contains both the original Round 1 baseline and a later Round 2 reconciliation section.

## Files

- `7d_snapshot.json`
  - Generated by `scripts/run_monthly_consolidation.py`
  - Contains the latest structured output from `scripts/verify_7d.py`
- `monthly_consolidation_report.json`
  - Generated by `scripts/run_monthly_consolidation.py`
  - Aggregates:
    - 7D gate result
    - memory hygiene
    - memory quality + learning sample generation
    - architecture boundary contract
    - docs consistency checks
    - persona swarm readiness check
- `repo_healthcheck_latest.json`
  - Generated by `scripts/run_repo_healthcheck.py`
  - Aggregates:
    - python lint (`ruff`)
    - python format check (`black --check`)
    - python tests (`pytest tests -q`)
    - web lint (`npm --prefix apps/web run lint`)
    - web tests (`npm --prefix apps/web run test`)
    - git hygiene (`scripts/verify_git_hygiene.py`)
    - dual-track boundary (`scripts/verify_dual_track_boundary.py --strict --staged`)
    - persona swarm readiness (`scripts/run_persona_swarm_framework.py --strict`)
    - skill registry contract (`scripts/verify_skill_registry.py --strict`)
    - multi-agent divergence (`scripts/run_multi_agent_divergence_report.py --strict`)
    - memory quality (`scripts/run_memory_quality_report.py --strict`)
    - memory governance contract (`scripts/run_memory_governance_contract_check.py --strict`)
    - friction shadow replay export (`scripts/run_friction_shadow_replay_export.py --strict`)
    - friction shadow calibration (`scripts/run_friction_shadow_calibration_report.py --strict`)
    - philosophical reflection (`scripts/run_philosophical_reflection_report.py --strict`)
    - memory topology fit (`scripts/run_memory_topology_fit_report.py --strict`)
    - 7D audit (`scripts/verify_7d.py`)
- `repo_healthcheck_latest.md`
  - Generated by `scripts/run_repo_healthcheck.py`
  - Human-readable summary of the latest healthcheck run
- `git_hygiene_latest.json`
  - Generated by `scripts/verify_git_hygiene.py`
  - Aggregates:
    - object-store snapshot (`git count-objects -vH`)
    - fsck diagnostics (`git fsck --no-reflogs`)
    - threshold evaluation (`max_loose_count`, `max_dangling`)
- `git_hygiene_latest.md`
  - Generated by `scripts/verify_git_hygiene.py`
  - Human-readable summary of the latest git hygiene snapshot
- `dual_track_boundary_latest.json`
  - Generated by `scripts/verify_dual_track_boundary.py`
  - Captures dual-repo boundary policy results for changed paths
  - Includes:
    - blocked prefix/file policy snapshot
    - changed file source metadata
    - violation list with matched policy rules
- `dual_track_boundary_latest.md`
  - Generated by `scripts/verify_dual_track_boundary.py`
  - Human-readable summary of latest dual-track boundary check
- `multi_persona_eval_latest.json`
  - Generated by multi-persona evaluation runs (Phase 78 protocol)
  - Tracks A/B/C comparison metrics and promotion decision snapshot
- `persona_swarm_framework_latest.json`
  - Generated by persona swarm framework validation runs (`scripts/run_persona_swarm_framework.py`)
  - Captures swarm coordination metrics, gate decisions, persona readiness diagnostics
  - Includes governance signals:
    - guardian fail-fast trigger state
    - cost profile tier and recommended agent budget
  - Includes runtime budget application fields:
    - `baseline_evaluation` (full signal pass)
    - `execution_plan` (selected agents after budget policy)
- `swarm_long_task_plan_latest.json`
  - Generated by long-task swarm planning runs (same framework, custom input profile)
  - Captures planning bootstrap snapshot:
    - decision (`approve` / `revise` / `block` / `defer`)
    - decision support, swarm score, safety/cost indices
    - selected archetype and next planning phase
- `external_source_registry_latest.json`
  - Generated by `scripts/run_external_source_registry_check.py`
  - Captures external source allowlist validation:
    - `https` scheme enforcement
    - blocked short-link host rejection
    - allowlist host enforcement
    - source `reviewed_at` freshness checks
    - `spec/open_source_apps.yaml` repo/website URL compliance
- `external_source_registry_latest.md`
  - Generated by `scripts/run_external_source_registry_check.py`
  - Human-readable summary of latest external source registry validation
- `multi_agent_divergence_latest.json`
  - Generated by `scripts/run_multi_agent_divergence_report.py`
  - Aggregates council transcript collaboration-contract divergence signals:
    - contract coverage (`contract_entry_count`)
    - role/risk/handoff distribution
    - invalid contract record diagnostics
    - high-risk handoff counts
- `multi_agent_divergence_latest.md`
  - Generated by `scripts/run_multi_agent_divergence_report.py`
  - Human-readable summary of latest multi-agent divergence report
- `memory_quality_latest.json`
  - Generated by `scripts/run_memory_quality_report.py`
  - Aggregates self-journal quality signals for failure learning loop:
    - `block` + `declare_stance` coverage
    - provenance/divergence/contract coverage rates
    - risk and uncertainty distribution
- `memory_quality_latest.md`
  - Generated by `scripts/run_memory_quality_report.py`
  - Human-readable summary of latest memory quality report
- `memory_learning_samples_latest.jsonl`
  - Generated by `scripts/run_memory_quality_report.py`
  - Sanitized learning-sample stream for `block` / `declare_stance` cases
  - Intended for training feedback loops without raw user prompt replay
- `memory_governance_contract_latest.json`
  - Generated by `scripts/run_memory_governance_contract_check.py`
  - Contract health snapshot for shared memory-governance schema/example
  - Includes check-by-check pass/fail/warn diagnostics
- `memory_governance_contract_latest.md`
  - Generated by `scripts/run_memory_governance_contract_check.py`
  - Human-readable summary of latest memory-governance contract validation
- `friction_shadow_replay_latest.json`
  - Generated by `scripts/run_friction_shadow_replay_export.py`
  - Replay dataset export quality snapshot for calibration inputs:
    - source coverage (`journal` / `discussion` / fallback synthetic)
    - tier balance (`free` / `premium`)
    - replay tension/friction distribution
    - replay-distribution drift checks versus previous snapshot
    - drift guard auto-skips when replay falls back to synthetic scenarios
- `friction_shadow_replay_latest.md`
  - Generated by `scripts/run_friction_shadow_replay_export.py`
  - Human-readable summary of latest replay export quality report
- `friction_shadow_calibration_latest.json`
  - Generated by `scripts/run_friction_shadow_calibration_report.py`
  - Captures shadow-vs-active routing calibration metrics:
    - route-change rate and council escalation delta
    - high-friction escape rate guard
    - friction-band level council routing distribution
- `friction_shadow_calibration_latest.md`
  - Generated by `scripts/run_friction_shadow_calibration_report.py`
  - Human-readable summary of latest friction shadow calibration report
- `philosophical_reflection_latest.json`
  - Generated by `scripts/run_philosophical_reflection_report.py`
  - Aggregates philosophical memory-governance signals:
    - reflection/conflict/choice event counts
    - high-tension event rate
    - unresolved-topic pressure
    - friction point extracts for council replay
- `philosophical_reflection_latest.md`
  - Generated by `scripts/run_philosophical_reflection_report.py`
  - Human-readable summary of latest philosophical reflection report
- `memory_topology_fit_latest.json`
  - Generated by `scripts/run_memory_topology_fit_report.py`
  - Pragmatic topology recommendation based on governance pressure and compute budget:
    - governance need score (`friction`, unresolved topics, identity-choice stability)
    - resource budget score (`max_vram_gb`, `max_latency_ms`, profile)
    - recommended topology (`flat` / `planar` / `hierarchical`) with fit ranking
- `memory_topology_fit_latest.md`
  - Generated by `scripts/run_memory_topology_fit_report.py`
  - Human-readable summary of latest topology fit recommendation

## Generation

```bash
python scripts/run_monthly_consolidation.py --strict
```

CI-friendly mode (when discussion files are absent in clean checkout):

```bash
python scripts/run_monthly_consolidation.py --strict --allow-missing-discussion
```

```bash
python scripts/run_repo_healthcheck.py --strict
```

> `run_repo_healthcheck.py` now includes git hygiene checks by default, so one run covers both code and object-store hygiene.

CI-friendly mode (skip `verify_7d` when discussion file is absent):

```bash
python scripts/run_repo_healthcheck.py --strict --allow-missing-discussion
```

Live SDH mode-switch smoke (requires running web/backend services):

```bash
python scripts/run_repo_healthcheck.py --strict --include-sdh --check-council-modes
```

Isolated-port variant (useful with `scripts/run_7d_isolated.py` defaults):

```bash
python scripts/run_repo_healthcheck.py --strict --include-sdh --check-council-modes --web-base http://127.0.0.1:3002 --api-base http://127.0.0.1:5001 --sdh-timeout 40
```

```bash
python scripts/verify_git_hygiene.py
```

Strict mode (optional, fail when thresholds are exceeded):

```bash
python scripts/verify_git_hygiene.py --strict --max-loose-count 5000 --max-dangling 50 --max-tracked-ignored 28
```

```bash
python scripts/run_persona_swarm_framework.py --strict
```

```bash
python scripts/run_persona_swarm_framework.py --input docs/experiments/persona_swarm_long_task_input_2026-03-01.json --out docs/status/persona_swarm_long_task_latest.json --strict
```

```bash
python scripts/run_swarm_long_task_planning.py --strict
```

```bash
python scripts/run_external_source_registry_check.py --strict
```

```bash
python scripts/verify_skill_registry.py --strict
```

```bash
python scripts/run_multi_agent_divergence_report.py --strict
```

```bash
python scripts/run_memory_quality_report.py --strict
```

```bash
python scripts/run_memory_governance_contract_check.py --strict
```

```bash
python scripts/run_friction_shadow_replay_export.py --strict
```

```bash
python scripts/run_friction_shadow_replay_export.py --strict --max-avg-tension-drift 0.35 --max-avg-friction-drift 0.35 --max-high-friction-rate-drift 0.40 --min-scenario-count-ratio 0.20
```

```bash
python scripts/run_friction_shadow_calibration_report.py --strict
```

```bash
python scripts/run_philosophical_reflection_report.py --strict
```

```bash
python scripts/run_memory_topology_fit_report.py --strict
```

```bash
python scripts/verify_dual_track_boundary.py --strict --staged
```

CI-style diff mode (compare against base ref):

```bash
python scripts/verify_dual_track_boundary.py --strict --base-ref origin/master
```

## Automation

- GitHub Actions workflow: `.github/workflows/monthly_consolidation.yml`
- GitHub Actions workflow: `.github/workflows/repo_healthcheck.yml`
- GitHub Actions workflow: `.github/workflows/git_hygiene.yml`
- GitHub Actions workflow: `.github/workflows/persona_swarm.yml`
- GitHub Actions workflow: `.github/workflows/external_source_registry.yml`
- GitHub Actions workflow: `.github/workflows/friction_shadow_calibration.yml`
- GitHub Actions workflow: `.github/workflows/dual_track_boundary.yml`
- Trigger modes:
  - monthly schedule (`30 3 1 * *`, UTC)
  - weekly schedule (`15 3 * * 1`, UTC)
  - manual dispatch (`workflow_dispatch`)
- Repo healthcheck manual dispatch supports SDH inputs:
  - `include_sdh` (boolean)
  - `web_base`, `api_base`, `sdh_timeout`
  - `check_council_modes` (boolean)
  - Dispatch orchestration script: `scripts/run_repo_healthcheck_dispatch.py`
  - Validation behavior:
    - invalid `sdh_timeout` (non-positive integer) blocks run
    - SDH inputs with `include_sdh=false` emit warning and are ignored
    - only one of `web_base` / `api_base` emits warning; missing side falls back to `verify_7d` default
- Persona swarm workflow dispatch:
  - inputs: `strict`, `input_path`
  - dispatch orchestration script: `scripts/run_persona_swarm_dispatch.py`
  - validation behavior:
    - missing `input_path` blocks run with workflow error annotation
    - non-`.json` input path emits warning but still runs
- Workflow uploads selected `docs/status/*.json`, `*.md`, and `*.jsonl` artifacts for audit/review.

## Convergence Cadence

- Convergence week: freeze new framework additions, prioritize hygiene and consistency.
- Minimum maintenance loop:
  - `python scripts/run_repo_healthcheck.py --strict --allow-missing-discussion`
  - `python scripts/run_7d_isolated.py --web-script dev` (when SDH live evidence is needed)
  - `python scripts/verify_git_hygiene.py`

## Notes

- These artifacts are intended as operational truth for CI/review workflows.
- Human-facing narrative docs (`README.md`, sync notes) should reference these artifacts instead of duplicating volatile numbers.
