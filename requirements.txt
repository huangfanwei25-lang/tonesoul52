# ToneSoul Architecture Engine - Dependencies
# Philosophy: Minimal Dependencies. Local-first.
#
# NOTE: This file mirrors pyproject.toml for compatibility.
# Canonical dependency source is pyproject.toml.
# Install via: pip install -e ".[dev]" (recommended)

# === Core Runtime (pyproject.toml [project.dependencies]) ===
flask>=2.3.0
flask-cors>=4.0.0
numpy>=1.24.0
pydantic>=2.0.0
pyyaml>=6.0
rich>=13.0.0
requests>=2.31.0
websockets>=12.0

# === Optional: LLM Providers ===
# Uncomment as needed:
# google-generativeai>=0.8.0   # For Gemini API
# sentence-transformers>=2.2.0  # For local embeddings
# chromadb>=0.4.0               # For vector DB

# === Optional: Dashboard (pip install -e ".[dashboard]") ===
# streamlit>=1.28.0
# plotly>=5.18.0
# pandas>=2.0.0

# === Optional: System Monitoring ===
# psutil>=5.9.0

# === Development (pip install -e ".[dev]") ===
pytest>=7.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.0
hypothesis>=6.0.0
black>=26.0.0,<27.0.0
ruff>=0.1
freezegun>=1.2.0
mypy>=1.0.0
types-requests
types-PyYAML
